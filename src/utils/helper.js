/**
 * Module for Huge algorithmic calculation
 * @module adds
 */

/**
 * Huge algorithmic calculation
 * just for getting started with unit testing
 *
 * @param {number} a first argument
 * @param {number} b second argument
 *
 * @returns {number} Heavy calculated result
 */
const adds = (a, b) => {
	return a + b;
};

module.exports = adds;
